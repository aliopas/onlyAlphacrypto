[phases.setup]
nixPkgs = ["...", "nodejs_18", "npm-9_x", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = ["npm run start"]

[variables]
NODE_ENV = "production"
NPM_FLAGS = "--production=false"
