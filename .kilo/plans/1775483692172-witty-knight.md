# Phase 6 - Resilience Layer Implementation Plan

## Objective
Create two new service files for the Resilience Layer in the OnlyAlpha platform: `circuitBreaker.service.ts` and `dynamicThreshold.service.ts`.

## Files to Create

### 1. backend/src/services/circuitBreaker.service.ts
- **Purpose**: Implements Circuit Breaker pattern for external service resilience
- **Dependencies**: None (standalone class)
- **Content**: Exact implementation as specified, including CircuitBreaker class and 4 exported singletons

### 2. backend/src/services/dynamicThreshold.service.ts
- **Purpose**: Provides dynamic thresholds based on news buffer activity and publication rates
- **Dependencies**: 
  - `db` from '../config/db'
  - `rawNewsBuffer`, `coinNews` from '../models/market.model'
  - `gte`, `and`, `sql`, `count` from 'drizzle-orm'
- **Content**: Exact implementation with two functions as specified

## Implementation Steps
1. Create `circuitBreaker.service.ts` with the CircuitBreaker class and singleton exports
2. Create `dynamicThreshold.service.ts` with the two async functions
3. Verify no TypeScript errors (run typecheck)
4. Ensure no linting issues (run lint)

## Verification
- All dependencies are confirmed to exist in the codebase
- Drizzle ORM is installed and available
- No external dependencies required for circuitBreaker
- Code follows existing patterns and TypeScript standards